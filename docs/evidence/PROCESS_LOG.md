# 过程证据日志

## 2026-08-24

- 从空工作区建立 React/Vite/TypeScript 工程；记录上游 andHW/lod 固定提交和 MIT 许可。
- 复制三张已确认概念图到 `docs/design/`；生成并筛选非文化丝线背景与透明梭子。
- 实现离线分类器、确定性 48×24 纹样、24 梭交替手势、双层 Canvas、结果 PNG、二维码 fragment 分享和静态分享页。
- 生成 1,440/360 条可审阅语料草案并训练 Float32 模型；指标保存在 `model-metrics.json`。
- 首次生产单文件构建通过；模型接入后的构建约 6.7MB，后续加入字体子集仍待复测。
- Vitest：5 个测试文件、14 个测试全部通过（分类、隐私/安全、纹样确定性、载荷校验、手势、24 行上限）。
- 权威资料核对：新华社页面确认两人协作及一天五六厘米的限定场景；中国非遗网页确认构图与纹样寓意。
- 1920×1080 Playwright 实测三屏；真实拖拽短滑不计数、长滑计 1 行；暂停/恢复、24 行结果、分享正误分支与 1080×1440 下载均通过。
- 离线生产单文件以 `file://`、网络关闭运行，外部请求为 0；5 次冷启动最大 340ms；分类冷时延 14.7ms。
- 30 轮加速计时状态/内存压力运行后回到输入屏，JS heap 无净增长；完整实时时序另跑通 1 轮。
- 冻结公开分享地址为 `https://zys1544526484.github.io/woven-wishes/share.html`，加入 GitHub Pages Actions 工作流，并以该地址重建离线单文件。
- RC2 浏览器回归：1920×1080 主流程 24 梭完成，结果二维码的 `data-share-url` 与冻结地址一致；390×844 分享页正确重建，损坏载荷在首访时进入双语错误页；控制台 0 error / 0 warning。
- RC2 断网 `file://` 回归：24 梭完成、结果页可见、外部 HTTP(S) 请求为 0；更新 `BUILD_HASHES.sha256`。

后续真实触屏、手机扫码、人测、专家回复、实拍视频和提交平台证据应继续追加，不得以“计划”冒充已完成证据。
