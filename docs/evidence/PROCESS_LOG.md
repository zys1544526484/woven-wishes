# 过程证据日志

## 2026-08-24

- 从空工作区建立 React/Vite/TypeScript 工程；记录上游 andHW/lod 固定提交和 MIT 许可。
- 复制三张已确认概念图到 `docs/design/`；生成并筛选非文化丝线背景与透明梭子。
- 实现离线分类器、确定性 48×24 纹样、24 梭交替手势、双层 Canvas、结果 PNG、二维码 fragment 分享和静态分享页。
- 生成 1,440/360 条可审阅语料草案并训练 Float32 模型；指标保存在 `model-metrics.json`。
- 首次生产单文件构建通过；模型接入后的构建约 6.7MB，后续加入字体子集仍待复测。
- Vitest：5 个测试文件、14 个测试全部通过（分类、隐私/安全、纹样确定性、载荷校验、手势、24 行上限）。
- 权威资料核对：新华社页面确认两人协作及一天五六厘米的限定场景；中国非遗网页确认构图与纹样寓意。
- 1920×1080 Playwright 实测三屏；真实拖拽短滑不计数、长滑计 1 行；暂停/恢复、24 行结果、分享正误分支与 1080×1440 下载均通过。
- 离线生产单文件以 `file://`、网络关闭运行，外部请求为 0；5 次冷启动最大 340ms；分类冷时延 14.7ms。
- 30 轮加速计时状态/内存压力运行后回到输入屏，JS heap 无净增长；完整实时时序另跑通 1 轮。
- 冻结公开分享地址为 `https://zys1544526484.github.io/woven-wishes/share.html`，加入 GitHub Pages Actions 工作流，并以该地址重建离线单文件。
- RC2 浏览器回归：1920×1080 主流程 24 梭完成，结果二维码的 `data-share-url` 与冻结地址一致；390×844 分享页正确重建，损坏载荷在首访时进入双语错误页；控制台 0 error / 0 warning。
- RC2 断网 `file://` 回归：24 梭完成、结果页可见、外部 HTTP(S) 请求为 0；更新 `BUILD_HASHES.sha256`。

后续真实触屏、手机扫码、人测、专家回复、实拍视频和提交平台证据应继续追加，不得以“计划”冒充已完成证据。

## 2026-08-25（v1.1 可读性与文化边界改版）

- 根据小白体验反馈重做三屏叙事：首屏加入体验所得与四步旅程；织造页显示“心愿→AI读懂→纹样选择”；结果页直接回答“为什么是这幅纹样”。
- 将重复小图块替换为八类单幅大型主纹样，保留48×24、逐纬显形与24次交替送梭；增加轮廓、显形、点亮三阶段反馈。
- 加入本地 Web Audio 柔和五声音阶声景、送梭丝线反馈、每六梭阶段音和完成和弦；无录音素材、无网络请求，可随时关闭。
- 依据 UNESCO 与中国非遗网补做文化边界：玩法只聚焦南京云锦木机妆花；用“拽花工按花本提经/织手织入彩纬与金线”解释协作；固定显示“织，不是绣”和“不代表其他织锦”。
- “团式”改为来源中可直接支持的“团花构图”；所有题材寓意继续区分资料依据与本作当代语义映射。
- agent-browser 1920×1080 复跑输入、第12梭和结果；无滚动条、无页面错误。生产单文件以 `file://` 完成24梭，外部 HTTP 请求0。
- Playwright生产包验收：保存卡1080×1440、远程资源请求0、页面错误0；Vitest为6个文件17项全通过。
- 生成 `一日六厘米_AI共织锦愿_v1.1-RC3.zip`（52,475,396 bytes），SHA-256：`CB0AA410451B3D55723DA0AD32873C1844B84752AB3E58D0C1B6148E54358E61`。

## 2026-08-26（v1.2 语言、色彩与变化性精修）

- 按体验反馈把三屏、分享页和1080×1440保存卡改为单语显示；语言切换同步更新页面 `lang` 属性，语言入口自身保留“中 / EN”。
- 首屏加入纯CSS流动丝线高光，并依据中国非遗网所列黄、红、绿、蓝、紫、棕色系建立数字色板；色值明确标记为本作屏幕近似，不作为传统染色标准。
- 结果页将固定的“先分清”辨析框替换为六条按心愿种子轮换的云锦小知识，并在结果页、分享页和保存卡保持一致。
- 将每类心愿的固定主纹扩展为八个基础纹样、四种构图、四种边饰、四套配色及次纹组合；同一句心愿与版本仍保持确定性。
- 把厚重矩形织块改为较细的纬线带和三道丝线高光，继续遵守48×24矩阵及24次逐行织造约束。
- agent-browser以1920×1080核验中文与英文首屏：页面无滚动、无错误覆盖层；英文界面除语言入口“中”外无中文正文；流丝动画位置随时间变化。
- Vitest 6个文件21项通过；生产单文件构建通过，`dist/index.html` 约7.22MB。离线 `file://` 完成24梭、结果PNG为1080×1440、远程请求0、页面错误0。
- 本轮遵照用户要求只维护体验链接，不生成新压缩包、不发布正式版本。

## 2026-08-26（v1.4 高密度纹样重构）

- 用户明确锁定原结果概念中的精细团花为全部纹样的质量标准；保存标准图到 `docs/concepts/v1.4/`，不再接受48×24粗格直接作为最终美术。
- 以标准图为风格与精度参考，生成并人工筛选流云、团花、竹、梅、双鱼、牡丹、喜鹊、桃八类高密度数字丝线母版；原始输出、完整提示词、选择路径和版权状态均入账。
- 保留48×24矩阵作为内部确定性织序与分享数据；新增高密度绘制层，按题材裁取母版、按四套云锦数字色板重新着色，并用24条纬向遮罩逐梭显现。
- 结果边框增加双层丝金/孔雀青框、连续波纹与两侧圆饰；“墨绿雪青”配色调整为“金翠雪青”，确保所有配色保留清晰金线主层级。
- 展项结果、分享页与1080×1440保存卡共用同一高密度绘制函数，避免网页精致但保存图退回粗格。
- 1920×1080生产单文件 `file://` 完成24梭；保存第12梭、结果页与保存卡证据。PNG为1080×1440、远程请求0、页面错误0。
- Vitest 6个文件24项通过；单文件约10.94MB，低于12MB目标。遵照用户要求不生成新压缩包、不发布正式版本。

## 2026-08-26（v1.5 全纹样明度复核）

- 针对结果纹样偏淡的问题建立临时检视页，逐一核验八类母版与四套配色共32种组合；确认问题来自统一着色公式，而非单张流云纹样偶发。
- 将细丝线最低不透明度由约9%提高到60%，重新调整暗线阈值、综合色亮度、饱和度与轻微金线辉光；同步提高组合构图中次纹样的可见度。
- 修正后再次查看全部32种组合，流云、团花、竹、梅、双鱼、牡丹、喜鹊与桃在四套配色下均能清楚辨认，仍保留深色织物底与主次层级。
- 以“平安”完整跑通输入、24梭、结果和保存卡：PNG为1080×1440、远程请求0、页面错误0；Vitest 6个文件24项通过，单文件约10.94MB。
- 临时检视页面在核验后删除；遵照用户要求不生成压缩包、不发布正式版本。

## 2026-08-29（v1.6 研究驱动型玩法精品化）

- 将主流程扩展为“写下心愿→选择数字花本→选择共织方式→24梭共织→结果”，首页用纯CSS动画演示经线开合、梭子穿行与纹样显现，并直接说明“一人提经，一人送梭”。
- 每次分析固定给出 A/B/C 三张可复现提案：两张单题材不同构图与一张双题材组合构图；三张提案的标题、布局、矩阵哈希和高密度绘制结果均不同，并逐张标出资料依据与当代映射边界。
- 新增“我来送梭、我来提经、两人共织”三种模式；单人送梭为默认快速入口，两人共织突出显示。补强长按、触点取消、失焦、多指误触和提经中断时不增加进度的处理。
- 将24梭分成地部成形、彩纬入纹、金线显花、边饰合拢四段；每六梭改变界面光色与本地合成音色，最后一梭增加800毫秒整体流光。
- 结果页、分享页和1080×1440保存卡建立“愿望→AI理解→玩家花本/配色选择→协作方式→最终锦愿”的因果链；分享载荷升级到V2，并保留V1解码兼容。
- 应用内浏览器以1920×1080核验首页、三张花本、角色选择、第12梭和结果页；英文首屏除语言入口“中”外无中文正文。
- 浏览器分别完整走通“你送梭·AI提经”和“你提经·AI送梭”24梭流程；双人模式的软件手势边界由单元测试覆盖，真实多点触屏仍列为外部待测项，不冒充完成。
- Vitest 6个文件27项通过；主构建与分享页构建通过。离线生产文件验收结果：PNG 1080×1440、远程请求0、页面错误0，证据写入 `artifacts/qa/v1.6/`。
- 本轮只保留本地体验服务，不生成压缩包、不发布正式版本。
- 依据现场反馈，在花本选择、角色选择和织造三个中途阶段加入单语“返回首页 / Return Home”按钮；点击后清空本轮心愿、织造进度和定时器，不保留半成品，声音开关状态在各页面保持一致。
- 将重叠的长正弦音与宽带梭声改为带呼吸间隔的抽象五声音景；音乐与交互音分总线，经轻量动态压缩后输出。梭声缩短至80毫秒并滤除闷响频段，完成和弦出现时背景自动降低，关闭声音会清理仍在播放的声源。
- 高密度绘制不再把完整纹样自上而下裁切：1—6梭分散显现数字地部，7—12梭显现彩纬层，13—18梭叠加金线层，19—24梭沿周缘闭合边饰。种子同时改变主纹尺度、偏移、镜像、边饰样式和起始方向。
- 首梭增加不阻断的幽灵梭子轨迹；短滑、反向、纵向偏移、触控取消、多点误触及双人提经松开均给出单语具体反馈且不计进度。每六梭显示一条与当前动作绑定的南京云锦微知识，并保留数字转译边界。
- 混合心愿达到次意图阈值时，三张花本分别以主意图、次意图、主次合景组织；结果页、分享页和保存卡同步显示“主要心意＋也听见”，并明确不代表固定历史寓意。
- 新增托管式 `打开最新体验.cmd`：服务停止后可一键恢复，所有响应禁用缓存；开发页以会话轮询和同源窗口广播在服务重启后自动刷新。该逻辑在生产构建中被完全移除，离线文件仍无 `fetch`。
- 修复慢速冷启动时“服务实际已启动但启动器先报失败”的竞态：启动器现在分开等待端口与作品标识、最长等待60秒，并复用已验证的托管PID。实测完全停服后3.10秒恢复，重复打开0.74秒且PID不变；再以两个独立PowerShell任务同时冷启动，二者均成功且最终只复用一个已校验PID；PID文件为空时也能安全替换并恢复服务。页面返回200、应用标识正确、`Cache-Control: no-store`。
- 在 `codex/gameplay-refinement` 建立本地Git保存点 `86e9894` 与精品化保存点 `f1b4174`；首次推送曾因无法连接 `github.com:443` 失败，网络恢复后已成功备份到 `origin/codex/gameplay-refinement`。未推送 `main`，未触发正式 Pages 发布。
- 统一复核：Vitest 8个文件38项通过；主单文件约13.39MB；1920×1080连续24梭到结果页、结果停留计时、四段文案、首梭提示与控制台0 error/0 warning通过。真实双人多点触屏仍待设备实测。

## 2026-09-01（花本自主选择与跨窗口预览持久化）

- 花本页不再把默认预览当成玩家选择：A、B、C 必须由玩家亲自点选后才能继续，页面会明确回显所选标题；由此强化“AI给出提案、玩家完成定稿”的因果关系。
- 分析过渡由2.2秒调为1.45秒，在保留不少于1.2秒叙事过渡的同时缩短等待；每梭提交动画调整为320毫秒，AI提经提示约160毫秒后就绪，降低重复操作的停顿感。
- 第6、12、18梭加入不阻断操作的中央阶段反馈，分别说明数字地部、彩纬入纹和金线显花；织纹区域在1080p中占据主要视觉面积，左右解释栏保持辅助层级。
- 手势新增全程纵向峰值校验，避免手指先大幅偏离轨道再回到终点被误判有效；补齐 `lostpointercapture` 与页面隐藏时的取消处理，异常触控不增加进度。
- 将本地体验服务升级为当前 Windows 用户的登录计划任务 `WovenWishesLocalPreview`，不再依赖某个 Codex 窗口或终端进程。实测任务状态为运行中，固定地址返回200且作品标识正确；启动器仍保留会话级后备方案。
- 应用内浏览器以1920×1080复核：首页无滚动；未主动选择花本时继续按钮禁用；选择B提案、进入默认送梭模式并完成6梭后，进度准确进入第7梭与“彩纬入纹”；中途返回首页后本轮心愿与进度清空；控制台0 error / 0 warning。
- Vitest 8个文件38项通过，生产单文件构建通过，`dist/index.html` 约13.40MB。真实双人多点触屏仍待设备实测，不将软件自动化结果冒充硬件验收。

## 2026-09-01（最终纹样精细化重绘）

- 定位最终纹样发虚的根因：约397×400像素的母版被大幅放大后，又被统一最低64%不透明度、强增亮和宽阴影叠加，导致细丝变成发白的粗描边，母版角饰也残留在主纹周围。
- 重写统一高密度绘制处理：按原始亮度与色度恢复不同粗细的丝线层级，金线与彩纬分别着色；降低泛光、增强高质量缩放并滤除弱噪点和母版角饰。结果页、织造页、分享页与1080×1440保存卡继续共用同一绘制函数。
- 底布增加更细的经纬交错纹理，连续边饰改为金翠双轨交织结构；结果展示框改为收敛的丝金装裱层，并加入低频、低透明度的丝面流光，减少普通网页边框感。
- 应用内浏览器以1920×1080完整完成“丰足→C花本→24梭→鱼跃花开”流程；鱼鳞、鳍线、旋涡、牡丹次纹与彩纬可以分别辨认，角落残框消失，页面无滚动，控制台0 error / 0 warning。
- Vitest 8个文件38项通过；生产单文件构建通过，`dist/index.html` 约13.40MB。未生成压缩包、未发布正式版本。
